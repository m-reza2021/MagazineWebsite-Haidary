from django.apps import AppConfig


class ZoomitCounterConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'zoomit_counter'
    verbose_name = 'تعداد بازدید پست ها'
